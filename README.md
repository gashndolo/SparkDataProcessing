# SparkDataProcessing


This practice project focuses on data transformation and integration using PySpark. 
We are working with two datasets to perform various transformations such as adding columns, 
renaming columns, dropping unnecessary columns, joining dataframes, and finally, writing the 
results into a Hive warehouse and an HDFS file system.